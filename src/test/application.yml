spring:
  application:
    name: order-service
  cloud:
    discovery:
      enabled: false
    service-registry:
      auto-registration:
        enabled: false
  jpa:
    database: hsql
    hibernate:
      ddl-auto: create-drop
    show-sql: true